import styled from "styled-components";

const HorizontalSeparator = styled.div`
  border-bottom: 1px solid ${(props) => props.theme.colors.horizontalSeparator};
  margin-bottom: 0.5rem;
`;
export default HorizontalSeparator;
